{
  "name": "quebec.covid19",
  "version": "1.0.0",
  "main": "index.html",
  "author": "Jason Rametta",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "parcel index.html",
    "build": "rm -rf dist && parcel build index.html",
    "deploy": "yarn build && cd .. && netlify deploy --prod"
  },
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "elm-hot": "^1.1.4",
    "netlify-cli": "^2.19.0",
    "node-elm-compiler": "^5.0.4",
    "parcel-bundler": "^1.12.4"
  }
}
